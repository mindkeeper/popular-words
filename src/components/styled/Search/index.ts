import styled from "styled-components";

const Search = styled.div`
  display: flex;
  align-items: center;
  flex-direction: row;
  min-width: 100%;
  gap: 1rem;
  position: relative;
`;
export default Search;
